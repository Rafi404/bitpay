import 'package:flutter/material.dart';

class ScanRecieptPage extends StatelessWidget {
  const ScanRecieptPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Scan & Receipt Page"),
      ),
    );
  }
}